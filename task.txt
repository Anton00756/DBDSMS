Кластерная система анализа и обработки данных
Cluster data analysis and processing system
[CDAPS by AE]

Логи по S3 с ошибкой; нужен ли опциональный сбор метрик внутри map (например, дедупликация: вход, выход;
риск: больше if`ов - больше нагрузки, проще дёрнуть отдельно сбор метрик [?])

Контейнер-оркестратор:
    Валидация yaml`a до старта flink`a / job`ы [аля health_check]
    Автогенерация по yaml`у для Grafana: для каждого потока все метрики или лучше разнести каждую метрику отдельно?

Source, sink;
Операции:
    Удаление из ряда, удаление из поля
    Изменение ряда, изменение поля
    Добавление ряда, добавление поля (Обогащение)
    Фильтрация
    Дедупликация
    Группировка по ключу
    Сбор метрик
    Объединение потоков [опционально]

! Удаление / изменение / добавление ряда, поля в одном map`е [схема "вход-выход"]